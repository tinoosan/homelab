apiVersion: apps/v1
kind: Deployment
metadata:
  name: aria2
spec:
  template:
    metadata:
      annotations:
        rollout/nonce: "2025-09-07T17:00:34Z"
    spec:
      nodeSelector:
        kubernetes.io/hostname: mugiwara

      containers:
        - name: aria2
          securityContext:
            runAsUser: 0
            runAsGroup: 0
          env:
            - name: PUID
              value: "65532"
            - name: PGID
              value: "65532"
          volumeMounts:
            - name: aria2-data
              mountPath: /data

      volumes:
        - name: aria2-data
          hostPath:
            path: /media/test
            type: Directory
