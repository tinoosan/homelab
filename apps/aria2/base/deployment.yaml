apiVersion: apps/v1
kind: Deployment
metadata:
  name: aria2
  labels:
    app: aria2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aria2
  template:
    metadata:
      labels:
        app: aria2
      annotations:
        homelab.dev/rollout-nonce: "2025-10-27-06"
    spec:
      volumes:
        - name: downloads-tv
          hostPath:
            path: /mnt/gdrive/Videos/TV
            type: DirectoryOrCreate
        - name: downloads-movies
          hostPath:
            path: /mnt/gdrive/Videos/Movies
            type: DirectoryOrCreate
      initContainers:
        - name: fix-permissions
          image: busybox:1.36
          securityContext:
            runAsUser: 0
            runAsGroup: 0
          command:
            - sh
            - -c
            - >-
              mkdir -p /downloads/tv /downloads/movies &&
              chown -R 65532:65532 /downloads/tv /downloads/movies &&
              chmod -R 775 /downloads/tv /downloads/movies
          volumeMounts:
            - name: downloads-tv
              mountPath: /downloads/tv
            - name: downloads-movies
              mountPath: /downloads/movies
      containers:
        - name: aria2
          image: p3terx/aria2-pro:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: rpc
              containerPort: 6800
          env:
            - name: RPC_SECRET
              valueFrom:
                secretKeyRef:
                  name: aria2-secrets
                  key: ARIA2_SECRET
            - name: RPC_PORT
              value: "6800"
            - name: ENABLE_RPC
              value: "true"
            - name: TZ
              value: "UTC"
            - name: PUID
              value: "65532"
            - name: PGID
              value: "65532"
            - name: UMASK_SET
              value: "022"
            - name: RPC_LISTEN_ALL
              value: "true"
            - name: DISK_CACHE
              value: "64M"
            - name: DISABLE_DELETE_EMPTY_DIR
              value: "true"
          volumeMounts:
            - name: downloads-tv
              mountPath: /downloads/tv
            - name: downloads-movies
              mountPath: /downloads/movies
