apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: keycloak
resources:
  - ../../base
  - backup-cronjob.yaml
  - configmap.yaml
patchesStrategicMerge:
  - patch-statefulset.yaml
